<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@group.errors.count, "error") %> prohibited
        this post from being saved:</h2>
    <ul>
    <% @group.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>

  <%= f.collection_select :users, @users, 
                        :id, :name, { include_blank: false },
                         { multiple: true } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>